-- [[ RENZO GURITA - XRAY GURITA V3.1 BUG FIX ]] --

local Players = game:GetService("Players")
local lp = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- 1. AUTO FLAT COLOR (FIX: LOCK WARNA IJO)
local function ApplyFlat(v)
    pcall(function()
        if v:IsA("BasePart") or v:IsA("MeshPart") then
            local color = v.Color
            -- Deteksi warna ijo (Rumput/Daun)
            local isGreen = (color.G > color.R + 0.1 and color.G > color.B + 0.1)
            local name = v.Name:lower()
            local isNature = name:find("grass") or name:find("leaf") or name:find("tree") or name:find("terrain")

            if not (isGreen or isNature) then
                v.Material = Enum.Material.Plastic
                v.Reflectance = 0
                v.CastShadow = false
                if v:IsA("MeshPart") then v.TextureID = "" end
            end
        end
    end)
end

task.spawn(function()
    local d = workspace:GetDescendants()
    for i = 1, #d do
        ApplyFlat(d[i])
        if i % 300 == 0 then task.wait() end
    end
end)
workspace.DescendantAdded:Connect(ApplyFlat)

-- 2. MASTER ESP & HOLDER ESP (FIX: TEXT BASED)
_G.ESPActive = false
_G.HolderActive = false

local function CreateMasterESP(plr)
    local Line = Drawing.new("Line")
    Line.Visible = false; Line.Color = Color3.fromRGB(0, 160, 255); Line.Thickness = 1
    
    local function Setup()
        local char = plr.Character or plr.CharacterAdded:Wait()
        local root = char:WaitForChild("HumanoidRootPart", 10)
        local head = char:WaitForChild("Head", 10)
        
        -- Tag Nama
        local bbg = Instance.new("BillboardGui", root)
        bbg.Name = "SmartTag"; bbg.AlwaysOnTop = true; bbg.Size = UDim2.new(0, 80, 0, 20); bbg.ExtentsOffset = Vector3.new(0, 3, 0)
        local tag = Instance.new("TextLabel", bbg); tag.Size = UDim2.new(1, 0, 1, 0); tag.BackgroundTransparency = 1
        tag.Text = plr.Name; tag.TextColor3 = Color3.new(1, 1, 0); tag.Font = Enum.Font.GothamBold; tag.TextStrokeTransparency = 0.5
        
        -- Holder Tag (Ganti ke Text biar gak bug)
        local hbg = Instance.new("BillboardGui", head)
        hbg.Name = "HolderTag"; hbg.AlwaysOnTop = true; hbg.Size = UDim2.new(0, 50, 0, 20); hbg.ExtentsOffset = Vector3.new(0, 4.5, 0)
        local htag = Instance.new("TextLabel", hbg); htag.Size = UDim2.new(1, 0, 1, 0); htag.BackgroundTransparency = 1
        htag.Text = "[ ITEM ]"; htag.TextColor3 = Color3.fromRGB(0, 255, 255); htag.Font = Enum.Font.GothamBold; htag.TextSize = 12; htag.Visible = false

        game:GetService("RunService").RenderStepped:Connect(function()
            if _G.ESPActive and char and root then
                local Pos, OnScreen = Camera:WorldToViewportPoint(root.Position)
                if OnScreen then
                    Line.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y); Line.To = Vector2.new(Pos.X, Pos.Y); Line.Visible = true
                    bbg.Enabled = true
                else Line.Visible = false; bbg.Enabled = false end
            else Line.Visible = false; bbg.Enabled = false end
            
            -- Holder Check (Kalo megang Tool atau ada objek nempel di tangan)
            if _G.HolderActive then
                local hasItem = char:FindFirstChildOfClass("Tool") or (char:FindFirstChild("Right Arm") and #char["Right Arm"]:GetChildren() > 0)
                htag.Visible = hasItem and true or false
            else htag.Visible = false end
        end)
    end
    task.spawn(Setup); plr.CharacterAdded:Connect(Setup)
end

-- [[ UI & XRAY TETEP SAMA ]] --
-- (Tinggal copy sisa UI dari script sebelumnya)
