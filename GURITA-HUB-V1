-- [[ GURITA HUB V1 - ULTIMATE SMOOTH FIXED XRAY ]] --

local Players = game:GetService("Players")
local lp = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local CG = game:GetService("CoreGui")

-- 1. SAVE SYSTEM
_G.GH_Settings = {
    ESP_Player_GH = false,
    Xray_GH = false,
    AutoCollect_GH = false,
    ESP_Egg_GH = false
}

-- 2. XRAY FIX (Anti Abu-Abu & Restore Color)
local function DoXray()
    task.spawn(function()
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") and not v.Parent:FindFirstChild("Humanoid") then
                local n = v.Name:lower()
                -- Target: Wall, Base, Floor, Pillar
                if n:find("wall") or n:find("base") or n:find("floor") or n:find("pillar") or n:find("part") then
                    if _G.GH_Settings.Xray_GH then
                        -- Simpan data asli sebelum diubah
                        if not v:FindFirstChild("GH_Originals") then
                            local folder = Instance.new("Configuration", v)
                            folder.Name = "GH_Originals"
                            local t = Instance.new("NumberValue", folder); t.Name = "Trans"; t.Value = v.Transparency
                            local m = Instance.new("StringValue", folder); m.Name = "Mat"; m.Value = v.Material.Name
                        end
                        v.Transparency = 0.6
                        -- Jangan paksa material plastik pas xray biar gak abu-abu
                    else
                        -- Restore ke asli
                        if v:FindFirstChild("GH_Originals") then
                            v.Transparency = v.GH_Originals.Trans.Value
                            v.Material = Enum.Material[v.GH_Originals.Mat.Value]
                            v.GH_Originals:Destroy()
                        end
                    end
                end
            end
        end
    end)
end

-- 3. ESP PLAYER (SMOOTH)
local function UpdateESP()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= lp then
            if _G.GH_Settings.ESP_Player_GH then
                if p.Character and p.Character:FindFirstChild("Head") and not p.Character.Head:FindFirstChild("ESPTag") then
                    local bg = Instance.new("BillboardGui", p.Character.Head)
                    bg.Name = "ESPTag"; bg.AlwaysOnTop = true; bg.Size = UDim2.new(0, 200, 0, 50); bg.ExtentsOffset = Vector3.new(0, 3, 0)
                    local lbl = Instance.new("TextLabel", bg)
                    lbl.Size = UDim2.new(1, 0, 1, 0); lbl.BackgroundTransparency = 1; lbl.TextColor3 = Color3.new(1, 1, 1)
                    lbl.Text = p.Name; lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 18; lbl.TextStrokeTransparency = 0
                end
            else
                if p.Character and p.Character:FindFirstChild("Head") and p.Character.Head:FindFirstChild("ESPTag") then
                    p.Character.Head.ESPTag:Destroy()
                end
            end
        end
    end
end

-- 4. UI CONSTRUCTION
if CG:FindFirstChild("GuritaHubV1_Smooth") then CG.GuritaHubV1_Smooth:Destroy() end
local SG = Instance.new("ScreenGui", CG); SG.Name = "GuritaHubV1_Smooth"

local GHTxtBtn = Instance.new("TextButton", SG)
GHTxtBtn.Size = UDim2.new(0, 180, 0, 35); GHTxtBtn.Position = UDim2.new(0.5, -90, 0.05, 0)
GHTxtBtn.BackgroundTransparency = 1; GHTxtBtn.Text = "GURITA HUB V1"; GHTxtBtn.Font = Enum.Font.GothamBold; GHTxtBtn.TextSize = 22; GHTxtBtn.TextStrokeTransparency = 0.6

local Colors = {Color3.fromRGB(0, 170, 255), Color3.fromRGB(255, 120, 0), Color3.fromRGB(255, 255, 0), Color3.fromRGB(170, 0, 255)}
task.spawn(function()
    local i = 1
    while true do
        TweenService:Create(GHTxtBtn, TweenInfo.new(1.5), {TextColor3 = Colors[i]}):Play()
        task.wait(1.5); i = i % #Colors + 1
    end
end)

local Main = Instance.new("Frame", SG)
Main.Size = UDim2.new(0, 450, 0, 320); Main.Position = UDim2.new(0.5, -225, 0.5, -160); Main.BackgroundColor3 = Color3.fromRGB(15, 15, 22); Main.Visible = false
local MainStroke = Instance.new("UIStroke", Main); MainStroke.Color = Color3.fromRGB(0, 150, 255); MainStroke.Thickness = 2
Instance.new("UICorner", Main)

local CloseBtn = Instance.new("TextButton", Main)
CloseBtn.Size = UDim2.new(0, 30, 0, 30); CloseBtn.Position = UDim2.new(1, -35, 0, 5); CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 50); CloseBtn.Text = "X"; CloseBtn.TextColor3 = Color3.new(1, 1, 1); Instance.new("UICorner", CloseBtn)
CloseBtn.MouseButton1Click:Connect(function() Main.Visible = false end)
GHTxtBtn.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)

local Sidebar = Instance.new("Frame", Main)
Sidebar.Size = UDim2.new(0, 130, 1, -60); Sidebar.Position = UDim2.new(0, 10, 0, 45); Sidebar.BackgroundTransparency = 1
Instance.new("UIListLayout", Sidebar).Padding = UDim.new(0, 8)

local Pages = Instance.new("Frame", Main)
Pages.Size = UDim2.new(1, -160, 1, -100); Pages.Position = UDim2.new(0, 150, 0, 45); Pages.BackgroundTransparency = 1

local function CreatePage(name)
    local p = Instance.new("ScrollingFrame", Pages); p.Name = name; p.Size = UDim2.new(1, 0, 1, 0); p.BackgroundTransparency = 1; p.Visible = false; p.ScrollBarThickness = 0
    Instance.new("UIListLayout", p).Padding = UDim.new(0, 8)
    return p
end

local PagePaska = CreatePage("PASKA"); local PageESP = CreatePage("ESP")
PagePaska.Visible = true

local function CreateSideBtn(txt, page)
    local b = Instance.new("TextButton", Sidebar)
    b.Size = UDim2.new(1, 0, 0, 40); b.BackgroundColor3 = Color3.fromRGB(25, 25, 35); b.Text = txt; b.TextColor3 = Color3.new(1, 1, 1); b.Font = Enum.Font.GothamBold; b.TextSize = 12; Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function() 
        for _, v in pairs(Pages:GetChildren()) do v.Visible = false end
        page.Visible = true 
    end)
end
CreateSideBtn("PASKA EVENT", PagePaska); CreateSideBtn("ESP", PageESP)

local function CreateToggle(parent, txt, var, callback)
    local b = Instance.new("TextButton", parent)
    b.Size = UDim2.new(1, 0, 0, 45); b.BackgroundColor3 = Color3.fromRGB(22, 22, 32)
    local function UpdateBtn()
        local s = _G.GH_Settings[var]; b.Text = txt..(s and ": ON" or ": OFF")
        b.TextColor3 = s and Color3.new(0, 1, 0) or Color3.new(1, 0, 0)
    end
    UpdateBtn()
    b.MouseButton1Click:Connect(function() _G.GH_Settings[var] = not _G.GH_Settings[var]; UpdateBtn(); if callback then callback() end end)
    Instance.new("UICorner", b)
end

-- [[ EXECUTION ]] --
CreateToggle(PagePaska, "Auto Collect Coin", "AutoCollect_GH")
CreateToggle(PagePaska, "ESP Egg Hunt", "ESP_Egg_GH")
CreateToggle(PageESP, "X-Ray Base", "Xray_GH", DoXray)
CreateToggle(PageESP, "ESP Player", "ESP_Player_GH", UpdateESP)

-- Loop Sinkronisasi
task.spawn(function()
    while true do
        if _G.GH_Settings.ESP_Player_GH then UpdateESP() end
        -- Xray biarkan jalan via toggle saja biar gak berat, tapi kita cek berkala buat part baru
        if _G.GH_Settings.Xray_GH then DoXray() end
        task.wait(3)
    end
end)
