-- [[ GURITA HUB V43 - ULTRA FLAT COLOR & SPEED LOAD ]] --

-- FITUR 1: FLAT COLOR (Ditaruh paling atas biar langsung kerja!)
local function ForceFlatColor()
    pcall(function()
        settings().Rendering.QualityLevel = 1
        game:GetService("Lighting").GlobalShadows = false
        game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(255, 255, 255) -- Biar map terang
        
        local function Clean(v)
            if v:IsA("BasePart") then
                v.Material = Enum.Material.Plastic
                v.Reflectance = 0
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v:Destroy()
            end
        end

        for _, v in pairs(workspace:GetDescendants()) do Clean(v) end
        workspace.DescendantAdded:Connect(function(v) task.wait(); Clean(v) end)
    end)
end
ForceFlatColor() -- EKSEKUSI SEKARANG JUGA

local lp = game.Players.LocalPlayer
local PlayerGui = lp:WaitForChild("PlayerGui")
local HttpService = game:GetService("HttpService")

-- 2. SAVE/LOAD SYSTEM
local FileName = "GuritaHubConfig.json"
local function LoadSettings()
    local success, res = pcall(function()
        if isfile and isfile(FileName) then return HttpService:JSONDecode(readfile(FileName)) end
    end)
    if success and res then return res end
    return { HuntingMode = false }
end

local SavedData = LoadSettings()
_G.HuntingMode = SavedData.HuntingMode

local function SaveSettings()
    if writefile then
        pcall(function() writefile(FileName, HttpService:JSONEncode({ HuntingMode = _G.HuntingMode })) end)
    end
end

-- 3. DAFTAR TARGET
local NotifHistory = {}
local SuperRare = {"hydra", "no my egg", "baskito", "churrito", "hopilikalika", "quackini", "harp"}
local NormalTargets = {"tralala", "eggdin", "easter", "sahur", "sky egg", "bunny egg"}

-- 4. ALARM & NOTIF (GABUNG)
local function PlayAlert(tipe)
    local s = Instance.new("Sound", workspace)
    s.SoundId = (tipe == "rare") and "rbxassetid://4590662766" or "rbxassetid://138090596"
    s.Volume = 1; s:Play(); s.Ended:Connect(function() s:Destroy() end)
end

local function KirimNotifRare(objName)
    if NotifHistory[objName] then return end
    NotifHistory[objName] = true 
    local sg = Instance.new("ScreenGui", PlayerGui); sg.Name = "GuritaNotif"; sg.DisplayOrder = 10
    local f = Instance.new("Frame", sg)
    f.Size = UDim2.new(0, 280, 0, 70); f.Position = UDim2.new(1, 50, 0.8, 0)
    f.BackgroundColor3 = Color3.fromRGB(15, 15, 15); Instance.new("UICorner", f)
    local t = Instance.new("TextLabel", f)
    t.Size = UDim2.new(1, -20, 1, 0); t.Position = UDim2.new(0, 15, 0, 0)
    t.Text = "RARE FOUND:\n" .. objName:upper(); t.TextColor3 = Color3.new(1, 0, 0)
    t.Font = Enum.Font.GothamBold; t.TextSize = 14; t.BackgroundTransparency = 1; t.TextXAlignment = "Left"
    PlayAlert("rare")
    f:TweenPosition(UDim2.new(1, -300, 0.8, 0), "Out", "Back", 0.5)
    task.delay(5, function() 
        if f then f:TweenPosition(UDim2.new(1, 50, 0.8, 0), "In", "Quad", 0.5) end
        task.wait(0.6); sg:Destroy()
    end)
end

-- 5. ESP LOGIC (ISLAND ONLY & FULL NAME)
local function PasangESP(obj)
    if obj:FindFirstChild("GuritaTag") then return end
    task.spawn(function()
        local name = ""
        for i = 1, 40 do
            if obj.Name ~= "Part" and obj.Name ~= "Model" and obj.Name ~= "" then
                name = obj.Name:lower()
                break
            end
            task.wait(0.1)
        end
        if name == "" then return end

        local pos = obj:IsA("Model") and obj:GetModelCFrame().p or obj.Position
        if pos.Y < 100 then return end 

        local tipe = nil
        for _, r in pairs(SuperRare) do if name:find(r) then tipe = "rare"; break end end
        if not tipe then
            for _, e in pairs(NormalTargets) do if name:find(e) then tipe = "normal"; break end end
        end

        if not tipe or name:find("brainrot") then return end
        if tipe == "rare" then KirimNotifRare(obj.Name) end

        local col = (tipe == "rare") and Color3.new(1, 0, 0) or Color3.fromRGB(0, 170, 255)
        local b = Instance.new("BillboardGui", obj)
        b.Name = "GuritaTag"; b.Size = UDim2.new(0, 200, 0, 50); b.AlwaysOnTop = true; b.ExtentsOffset = Vector3.new(0, 6, 0)
        local t = Instance.new("TextLabel", b)
        t.Size = UDim2.new(1, 0, 1, 0); t.BackgroundTransparency = 1; t.TextColor3 = col; t.Font = Enum.Font.GothamBlack; t.TextSize = 16; t.TextStrokeTransparency = 0
        
        while obj and obj.Parent and _G.HuntingMode do
            local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                t.Text = obj.Name:upper() .. "\n[" .. math.floor((hrp.Position - pos).Magnitude) .. "s]"
            end
            task.wait(1)
        end
        if b then b:Destroy() end
    end)
end

-- 6. UI MENU
if PlayerGui:FindFirstChild("GuritaHubUI") then PlayerGui.GuritaHubUI:Destroy() end
local SG = Instance.new("ScreenGui", PlayerGui); SG.Name = "GuritaHubUI"; SG.ResetOnSpawn = false; SG.DisplayOrder = 9
local Toggle = Instance.new("TextButton", SG)
Toggle.Size = UDim2.new(0, 70, 0, 70); Toggle.Position = UDim2.new(0, 15, 0.45, 0)
Toggle.BackgroundColor3 = Color3.fromRGB(0, 170, 255); Toggle.Text = "G"; Toggle.TextColor3 = Color3.new(1,1,1); Toggle.Font = Enum.Font.GothamBlack; Toggle.TextSize = 35; Instance.new("UICorner", Toggle).CornerRadius = UDim.new(0, 15)
local Main = Instance.new("Frame", SG)
Main.Size = UDim2.new(0, 280, 0, 150); Main.Position = UDim2.new(0.5, -140, 0.5, -75); Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25); Main.Visible = false; Instance.new("UICorner", Main)
local BtnMaster = Instance.new("TextButton", Main)
BtnMaster.Size = UDim2.new(0, 240, 0, 70); BtnMaster.Position = UDim2.new(0, 20, 0, 60); BtnMaster.Font = Enum.Font.GothamBold; BtnMaster.TextColor3 = Color3.new(1,1,1); BtnMaster.TextSize = 18; Instance.new("UICorner", BtnMaster)

local function UpdateUI()
    BtnMaster.Text = _G.HuntingMode and "STATUS: ACTIVE" or "STATUS: OFF"
    BtnMaster.BackgroundColor3 = _G.HuntingMode and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(150, 0, 0)
end

UpdateUI()
Toggle.MouseButton1Down:Connect(function() Main.Visible = not Main.Visible end)
BtnMaster.MouseButton1Down:Connect(function()
    _G.HuntingMode = not _G.HuntingMode; UpdateUI(); SaveSettings()
    if _G.HuntingMode then for _, v in pairs(workspace:GetDescendants()) do PasangESP(v) end else NotifHistory = {} end
end)

-- EKSEKUSI ESP
if _G.HuntingMode then for _, v in pairs(workspace:GetDescendants()) do PasangESP(v) end end
workspace.DescendantAdded:Connect(function(v) if _G.HuntingMode then PasangESP(v) end end)
