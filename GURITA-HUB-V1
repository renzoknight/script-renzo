-- [[ GURITA-HUB-V1: SQUARE LOGO + SIDEBAR NAVIGATION ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Bersihkan versi lama
local old = PlayerGui:FindFirstChild("GuritaHub_Official")
if old then old:Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GuritaHub_Official"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true

-- --- 1. LOGO GH (KOTAK) ---
local LogoButton = Instance.new("TextButton")
LogoButton.Name = "GH_Trigger"
LogoButton.Parent = ScreenGui
LogoButton.Size = UDim2.new(0, 50, 0, 50)
LogoButton.Position = UDim2.new(0.5, -25, 0, 45)
LogoButton.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
LogoButton.Text = "GH"
LogoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoButton.Font = Enum.Font.GothamBold
LogoButton.TextSize = 20
LogoButton.ZIndex = 100

local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(0, 8) -- Kotak dengan sedikit lengkungan
LogoCorner.Parent = LogoButton

local LogoStroke = Instance.new("UIStroke")
LogoStroke.Parent = LogoButton
LogoStroke.Thickness = 2
LogoStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

-- --- 2. MAIN FRAME ---
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
MainFrame.Position = UDim2.new(0.5, -225, 0.5, -140)
MainFrame.Size = UDim2.new(0, 450, 0, 280)
MainFrame.Visible = false
MainFrame.ZIndex = 90

local UICornerMain = Instance.new("UICorner")
UICornerMain.CornerRadius = UDim.new(0, 10)
UICornerMain.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Parent = MainFrame
MainStroke.Thickness = 2

-- --- 3. JUDUL TENGAH (GURITA-HUB) ---
local CenterTitle = Instance.new("TextLabel")
CenterTitle.Parent = MainFrame
CenterTitle.BackgroundTransparency = 1
CenterTitle.Size = UDim2.new(1, 0, 0, 40)
CenterTitle.Position = UDim2.new(0, 0, 0, 5)
CenterTitle.Font = Enum.Font.GothamBold
CenterTitle.Text = "GURITA-HUB"
CenterTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
CenterTitle.TextSize = 16
CenterTitle.ZIndex = 95

-- --- 4. SIDEBAR MENU (KIRI) ---
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
Sidebar.Size = UDim2.new(0, 130, 1, 0)

local UICornerSide = Instance.new("UICorner")
UICornerSide.CornerRadius = UDim.new(0, 10)
UICornerSide.Parent = Sidebar

local ButtonContainer = Instance.new("Frame")
ButtonContainer.Parent = Sidebar
ButtonContainer.BackgroundTransparency = 1
ButtonContainer.Size = UDim2.new(1, -10, 1, -50)
ButtonContainer.Position = UDim2.new(0, 5, 0, 45)

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = ButtonContainer
UIListLayout.Padding = UDim.new(0, 5)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- Fungsi untuk buat tombol sidebar seperti di foto
local function createNavButton(name)
    local btn = Instance.new("TextButton")
    btn.Name = name.."Btn"
    btn.Parent = ButtonContainer
    btn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    btn.Size = UDim2.new(1, 0, 0, 32)
    btn.Font = Enum.Font.GothamBold
    btn.Text = name
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.TextSize = 12
    
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 6)
    c.Parent = btn
    return btn
end

-- Tombol-tombol sesuai foto
createNavButton("Features")
createNavButton("Protection")
createNavButton("ESP")
createNavButton("Utilities")
createNavButton("Customize")

-- --- 5. CLOSE BUTTON ---
local CloseButton = Instance.new("TextButton")
CloseButton.Parent = MainFrame
CloseButton.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
CloseButton.Position = UDim2.new(1, -30, 0, 10)
CloseButton.Size = UDim2.new(0, 22, 0, 22)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 4)
CloseCorner.Parent = CloseButton

-- --- 6. DRAGGING & RGB (OPTIMIZED) ---
local function MakeDraggable(ui)
    local dragging, dragInput, dragStart, startPos
    ui.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true; dragStart = input.Position; startPos = ui.Position
        end
    end)
    ui.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)
    RunService.Heartbeat:Connect(function()
        if dragging and dragInput then
            local delta = dragInput.Position - dragStart
            ui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end
MakeDraggable(MainFrame)
MakeDraggable(LogoButton)

local colors = {
    Color3.fromRGB(0, 160, 255), Color3.fromRGB(255, 120, 0),
    Color3.fromRGB(255, 210, 0), Color3.fromRGB(160, 0, 255)
}

task.spawn(function()
    local cIndex = 1
    while true do
        local nIndex = cIndex % #colors + 1
        for i = 0, 1, 0.02 do
            local lerpColor = colors[cIndex]:Lerp(colors[nIndex], i)
            MainStroke.Color = lerpColor
            LogoStroke.Color = lerpColor
            task.wait(0.04)
        end
        cIndex = nIndex
    end
end)

LogoButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
CloseButton.MouseButton1Click:Connect(function() MainFrame.Visible = false end)
