-- [[ GURITA HUB - ULTIMATE FORCE UI ]] --

local lp = game.Players.LocalPlayer
local PlayerGui = lp:WaitForChild("PlayerGui")

-- 1. DAFTAR TARGET
local TargetEggs = {"hydra bunny", "noo my eggs", "hopilikalika", "baskito", "churrito", "quackini", "snackini", "bunny bunny sahur"}

-- 2. UI FORCE (SISTEM PAKSA)
if PlayerGui:FindFirstChild("GuritaHubUI") then PlayerGui.GuritaHubUI:Destroy() end

local SG = Instance.new("ScreenGui", PlayerGui)
SG.Name = "GuritaHubUI"
SG.DisplayOrder = 999999999
SG.IgnoreGuiInset = true -- Mengabaikan area aman (Full Screen)

-- Tombol Utama
local Toggle = Instance.new("TextButton", SG)
Toggle.Size = UDim2.new(0, 70, 0, 70)
Toggle.Position = UDim2.new(0, 10, 0.4, 0)
Toggle.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
Toggle.Text = "G"
Toggle.Font = Enum.Font.GothamBlack
Toggle.TextSize = 35
Toggle.TextColor3 = Color3.new(1, 1, 1)
Toggle.ZIndex = 1000000000
Toggle.Active = true
Toggle.Draggable = true -- BISA DI GESER (Biar tau kalau UI-nya aktif)
Toggle.Modal = true -- PAKSA MENGUNCI INPUT GAME

Instance.new("UICorner", Toggle).CornerRadius = UDim.new(1, 0)

-- Main Frame
local MainFrame = Instance.new("Frame", SG)
MainFrame.Size = UDim2.new(0, 250, 0, 150)
MainFrame.Position = UDim2.new(0.5, -125, 0.5, -75)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Visible = false
MainFrame.ZIndex = 1000000001
Instance.new("UICorner", MainFrame)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 1, 0)
Title.Text = "GURITA HUB\nSTATUS: BERHASIL DIBUKA"
Title.TextColor3 = Color3.fromRGB(0, 170, 255)
Title.Font = Enum.Font.GothamBlack
Title.BackgroundTransparency = 1
Title.TextSize = 16

-- [[ LOGIKA KLIK DENGAN BANYAK EVENT ]] --
local function OpenMenu()
    MainFrame.Visible = not MainFrame.Visible
    print("GURITA HUB: Berhasil Klik!")
end

Toggle.MouseButton1Down:Connect(OpenMenu) -- Event 1
Toggle.Activated:Connect(OpenMenu) -- Event 2 (Cadangan)

-- 3. GLOBAL FLAT (LITE)
task.spawn(function()
    settings().Rendering.QualityLevel = 1
    game:GetService("Lighting").GlobalShadows = false
    local ms = game:GetService("MaterialService")
    pcall(function()
        ms.Use2022Materials = false
        for _, v in pairs(ms:GetChildren()) do v:Destroy() end
    end)
end)

-- 4. ESP (DIJALANKAN PELAN-PELAN BIAR GAK FREEZE)
local function BuatESP(obj)
    if obj:FindFirstChild("GuritaMarker") then return end
    pcall(function()
        local h = Instance.new("Highlight", obj)
        h.Name = "GuritaMarker"; h.FillColor = Color3.fromRGB(0, 170, 255); h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop 
    end)
end

task.spawn(function()
    local all = workspace:GetDescendants()
    for i, v in pairs(all) do
        local name = v.Name:lower()
        for _, target in pairs(TargetEggs) do
            if name == target then BuatESP(v) end
        end
        if i % 200 == 0 then task.wait() end -- Sangat santai biar Menu gak lag
    end
end)
