-- [[ GURITA HUB - FORCE LOAD VERSION ]] --
-- Fokus: Langsung Jalan Tanpa Mikir Save-an (Biar Gak Error)

local lp = game.Players.LocalPlayer
local PlayerGui = lp:WaitForChild("PlayerGui")

-- DAFTAR TELUR (Wajib Huruf Kecil)
local TargetEggs = {"hydra bunny", "noo my eggs", "hopilikalika", "baskito", "churrito", "quackini", "snackini", "bunny bunny sahur"}

-- FUNGSI ESP (ALWAYS ON TOP)
local function PasangESP(obj)
    if obj:FindFirstChild("GuritaMarker") then return end
    pcall(function()
        local h = Instance.new("Highlight", obj)
        h.Name = "GuritaMarker"; h.FillColor = Color3.fromRGB(0, 170, 255); h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop 
        
        local b = Instance.new("BillboardGui", obj)
        b.Name = "GuritaTag"; b.Size = UDim2.new(0, 150, 0, 50); b.AlwaysOnTop = true; b.ExtentsOffset = Vector3.new(0, 6, 0)
        
        local t = Instance.new("TextLabel", b)
        t.Size = UDim2.new(1, 0, 1, 0); t.BackgroundTransparency = 1; t.TextColor3 = Color3.fromRGB(0, 170, 255); t.Font = Enum.Font.GothamBlack; t.TextSize = 14; t.TextStrokeTransparency = 0
        
        task.spawn(function()
            while obj and obj.Parent do
                if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                    local pos = obj:IsA("Model") and obj:GetModelCFrame().p or obj.Position
                    t.Text = obj.Name:upper() .. "\n[" .. math.floor((lp.Character.HumanoidRootPart.Position - pos).Magnitude) .. " Studs]"
                end
                task.wait(1.5)
            end
        end)
    end)
end

-- FUNGSI FLAT COLOR (SANGAT ENTENG)
local function SikatPolos(v)
    pcall(function()
        if v:IsA("BasePart") and not v.Parent:FindFirstChild("Humanoid") then
            v.Material = Enum.Material.Plastic
            v.CastShadow = false
            if v:IsA("MeshPart") then v.TextureID = "" end
            for _, child in pairs(v:GetChildren()) do
                if child:IsA("SurfaceAppearance") or child:IsA("Texture") or child:IsA("Decal") then
                    child:Destroy()
                end
            end
        end
    end)
end

-- EKSEKUSI (Dikasih Jeda biar HP Gak Panas)
task.spawn(function()
    local all = workspace:GetDescendants()
    for i, v in pairs(all) do
        SikatPolos(v)
        local name = v.Name:lower()
        for _, target in pairs(TargetEggs) do
            if name == target then PasangESP(v) end
        end
        if i % 100 == 0 then task.wait(0.01) end -- Biar Redmi 10 gak freeze
    end
end)

-- Deteksi Spawn Baru
workspace.DescendantAdded:Connect(function(v)
    local name = v.Name:lower()
    for _, target in pairs(TargetEggs) do
        if name == target then task.wait(1) PasangESP(v) end
    end
    if v:IsA("BasePart") then SikatPolos(v) end
end)

-- UI MENU SEDERHANA (PASTI MUNCUL)
if PlayerGui:FindFirstChild("GuritaHubUI") then PlayerGui.GuritaHubUI:Destroy() end
local SG = Instance.new("ScreenGui", PlayerGui); SG.Name = "GuritaHubUI"; SG.DisplayOrder = 9999; SG.ResetOnSpawn = false

local Toggle = Instance.new("TextButton", SG)
Toggle.Size = UDim2.new(0, 60, 0, 60); Toggle.Position = UDim2.new(0, 10, 0.4, 0)
Toggle.BackgroundColor3 = Color3.fromRGB(0, 170, 255); Toggle.Text = "G"; Toggle.Font = Enum.Font.GothamBlack
Toggle.TextColor3 = Color3.new(1, 1, 1); Toggle.TextSize = 30; Instance.new("UICorner", Toggle).CornerRadius = UDim.new(1, 0)

local MainFrame = Instance.new("Frame", SG)
MainFrame.Size = UDim2.new(0, 200, 0, 100); MainFrame.Position = UDim2.new(0.5, -100, 0.5, -50)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20); MainFrame.Visible = false; Instance.new("UICorner", MainFrame)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 1, 0); Title.Text = "GURITA HUB ACTIVE"; Title.TextColor3 = Color3.fromRGB(0, 170, 255)
Title.Font = Enum.Font.GothamBlack; Title.TextSize = 16; Title.BackgroundTransparency = 1

Toggle.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

print("GURITA HUB: Force Loaded!")
