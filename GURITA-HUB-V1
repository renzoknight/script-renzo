-- [[ GURITA-HUB-V1: COMBINED EASTER EDITION ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Bersihkan versi sebelumnya
if PlayerGui:FindFirstChild("GuritaHub_Official") then
    PlayerGui.GuritaHub_Official:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GuritaHub_Official"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- --- DATA UNIT EASTER (LIST KAMU) ---
local EasterList = {
    "Hydra Bunny", "Hopilikalika Hopilikalako", "Churrito Bunnito", 
    "Baskito", "Los Bunitos", "Easter Easter Easter Sahur", 
    "Eggdin Egg Egg Dun", "Bunny Tralala", "Buntteo", 
    "Bunny Bunny Bunny Sahur", "Noo my Eggs"
}

local ESP_Enabled = false

-- --- 1. LOGO GH (KOTAK) ---
local LogoButton = Instance.new("TextButton")
LogoButton.Parent = ScreenGui
LogoButton.Size = UDim2.new(0, 50, 0, 50)
LogoButton.Position = UDim2.new(0.5, -25, 0, 50)
LogoButton.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
LogoButton.Text = "GH"
LogoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoButton.Font = Enum.Font.GothamBold
LogoButton.TextSize = 20
LogoButton.ZIndex = 1000
Instance.new("UICorner", LogoButton).CornerRadius = UDim.new(0, 8)
local LogoStroke = Instance.new("UIStroke", LogoButton)
LogoStroke.Thickness = 2

-- --- 2. MAIN FRAME ---
local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
MainFrame.Position = UDim2.new(0.5, -225, 0.5, -140)
MainFrame.Size = UDim2.new(0, 450, 0, 280)
MainFrame.Visible = false
MainFrame.ZIndex = 100
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Thickness = 2

-- --- 3. JUDUL (GURITA-HUB) ---
local CenterTitle = Instance.new("TextLabel")
CenterTitle.Parent = MainFrame
CenterTitle.Size = UDim2.new(1, 0, 0, 45)
CenterTitle.BackgroundTransparency = 1
CenterTitle.Text = "GURITA-HUB"
CenterTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
CenterTitle.Font = Enum.Font.GothamBold
CenterTitle.TextSize = 18
CenterTitle.ZIndex = 150

-- --- 4. SIDEBAR MENU (KIRI) ---
local Sidebar = Instance.new("Frame")
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
Sidebar.Position = UDim2.new(0, 10, 0, 45)
Sidebar.Size = UDim2.new(0, 130, 0, 225)
Sidebar.ZIndex = 110
Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 8)

local SidebarLayout = Instance.new("UIListLayout", Sidebar)
SidebarLayout.Padding = UDim.new(0, 5)
SidebarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function addSideBtn(name)
    local b = Instance.new("TextButton")
    b.Parent = Sidebar
    b.Size = UDim2.new(0.9, 0, 0, 35)
    b.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    b.Text = name
    b.TextColor3 = Color3.fromRGB(200, 200, 200)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 10
    b.ZIndex = 120
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
    return b
end

addSideBtn("Features GH")
addSideBtn("Protection GH")
addSideBtn("ESP GH")
local PaskahTab = addSideBtn("Paskah Event GH") -- Menu Sesuai Foto

-- --- 5. AREA FITUR (KANAN) ---
local Content = Instance.new("ScrollingFrame")
Content.Parent = MainFrame
Content.Position = UDim2.new(0, 150, 0, 45)
Content.Size = UDim2.new(1, -160, 0, 225)
Content.BackgroundTransparency = 1
Content.CanvasSize = UDim2.new(0, 0, 2, 0)
Content.ScrollBarThickness = 2
Content.ZIndex = 110

local ContentLayout = Instance.new("UIListLayout", Content)
ContentLayout.Padding = UDim.new(0, 8)
ContentLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- --- 6. LOGIKA ESP EGG HUNT ---
local function CreateESP(part, name)
    if part:FindFirstChild("GH_EasterESP") then return end
    local b = Instance.new("BillboardGui", part)
    b.Name = "GH_EasterESP"
    b.AlwaysOnTop = true
    b.Size = UDim2.new(0, 100, 0, 50)
    local t = Instance.new("TextLabel", b)
    t.BackgroundTransparency = 1
    t.Size = UDim2.new(1, 0, 1, 0)
    t.Text = name .. " GH"
    t.TextColor3 = Color3.fromRGB(255, 255, 0)
    t.Font = Enum.Font.GothamBold
    t.TextSize = 12
    t.TextStrokeTransparency = 0
end

task.spawn(function()
    while task.wait(1) do
        if ESP_Enabled then
            for _, obj in pairs(workspace:GetDescendants()) do
                for _, eggName in pairs(EasterList) do
                    if obj.Name == eggName and (obj:IsA("BasePart") or obj:IsA("Model")) then
                        local target = obj:IsA("Model") and obj.PrimaryPart or obj
                        if target then CreateESP(target, eggName) end
                    end
                end
            end
        else
            for _, v in pairs(workspace:GetDescendants()) do
                if v.Name == "GH_EasterESP" then v:Destroy() end
            end
        end
    end
end)

-- --- 7. TOMBOL FITUR GH ---
local function createFeatureBtn(text)
    local btn = Instance.new("TextButton", Content)
    btn.Size = UDim2.new(0.9, 0, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.Text = text .. " GH: OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.ZIndex = 120
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

local EasterESPBtn = createFeatureBtn("Easter ESP")

EasterESPBtn.MouseButton1Click:Connect(function()
    ESP_Enabled = not ESP_Enabled
    EasterESPBtn.Text = "Easter ESP GH: " .. (ESP_Enabled and "ON" or "OFF")
    EasterESPBtn.TextColor3 = ESP_Enabled and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 255, 255)
end)

-- --- 8. SISTEM DRAG & RGB ---
local function MakeDraggable(ui)
    local d, i, s, sp
    ui.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then d = true; s = input.Position; sp = ui.Position end end)
    ui.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement then i = input end end)
    UserInputService.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then d = false end end)
    RunService.Heartbeat:Connect(function() if d and i then local delta = i.Position - s; ui.Position = UDim2.new(sp.X.Scale, sp.X.Offset + delta.X, sp.Y.Scale, sp.Y.Offset + delta.Y) end end)
end
MakeDraggable(MainFrame); MakeDraggable(LogoButton)

local cols = {Color3.fromRGB(0, 160, 255), Color3.fromRGB(255, 120, 0), Color3.fromRGB(255, 210, 0), Color3.fromRGB(160, 0, 255)}
task.spawn(function()
    local c = 1
    while true do
        local n = c % #cols + 1
        for i = 0, 1, 0.05 do
            local l = cols[c]:Lerp(cols[n], i)
            MainStroke.Color = l; LogoStroke.Color = l
            task.wait(0.05)
        end
        c = n
    end
end)

LogoButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
local X = Instance.new("TextButton", MainFrame)
X.Size = UDim2.new(0, 25, 0, 25); X.Position = UDim2.new(1, -30, 0, 10); X.Text = "X"; X.BackgroundColor3 = Color3.fromRGB(150, 0, 0); X.TextColor3 = Color3.fromRGB(255, 255, 255); X.ZIndex = 200
X.MouseButton1Click:Connect(function() MainFrame.Visible = false end)
Instance.new("UICorner", X).CornerRadius = UDim.new(0, 6)
