local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GuritaHubGui"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- --- 1. LOGO "GH" (Tetap Ada di Layar) ---
local LogoButton = Instance.new("TextButton")
LogoButton.Name = "LogoGH"
LogoButton.Parent = ScreenGui
LogoButton.Size = UDim2.new(0, 50, 0, 50)
LogoButton.Position = UDim2.new(0.5, -25, 0, 10)
LogoButton.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
LogoButton.Text = "GH"
LogoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoButton.Font = Enum.Font.GothamBold
LogoButton.TextSize = 20
LogoButton.ZIndex = 10

local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(1, 0)
LogoCorner.Parent = LogoButton

local LogoStroke = Instance.new("UIStroke")
LogoStroke.Parent = LogoButton
LogoStroke.Thickness = 2
LogoStroke.Color = Color3.fromRGB(0, 150, 255)

-- --- 2. MAIN FRAME (MENU) ---
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -175)
MainFrame.Size = UDim2.new(0, 500, 0, 350)
MainFrame.Visible = false 
MainFrame.Active = true
MainFrame.Draggable = true 

local UICornerMain = Instance.new("UICorner")
UICornerMain.CornerRadius = UDim.new(0, 12)
UICornerMain.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Parent = MainFrame
MainStroke.Thickness = 3
MainStroke.Color = Color3.fromRGB(0, 150, 255)

-- --- 3. TOMBOL CLOSE (Di Pojok Kanan Atas Menu) ---
local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseBtn"
CloseButton.Parent = MainFrame
CloseButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60) -- Flat Red
CloseButton.Position = UDim2.new(1, -35, 0, 10)
CloseButton.Size = UDim2.new(0, 25, 0, 25)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 14

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseButton

-- --- 4. SIDEBAR KIRI ---
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Sidebar.Size = UDim2.new(0, 130, 1, 0)

local UICornerSidebar = Instance.new("UICorner")
UICornerSidebar.CornerRadius = UDim.new(0, 12)
UICornerSidebar.Parent = Sidebar

local SidebarTitle = Instance.new("TextLabel")
SidebarTitle.Parent = Sidebar
SidebarTitle.Size = UDim2.new(1, 0, 0, 50)
SidebarTitle.Position = UDim2.new(0, 0, 0, 15)
SidebarTitle.BackgroundTransparency = 1
SidebarTitle.Text = "GURITA-HUB-V1"
SidebarTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
SidebarTitle.Font = Enum.Font.GothamBold
SidebarTitle.TextSize = 12

-- --- 5. LOGIKA TOMBOL (OPEN/CLOSE) ---
LogoButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = true -- Membuka menu
end)

CloseButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false -- Menutup menu (
