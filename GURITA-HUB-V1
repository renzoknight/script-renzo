-- [[ GURITA-HUB-V1: TAB SYSTEM REPAIRED ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

if PlayerGui:FindFirstChild("GuritaHub_Official") then
    PlayerGui.GuritaHub_Official:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GuritaHub_Official"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- --- DATA ---
local EasterList = {"Hydra Bunny", "Hopilikalika Hopilikalako", "Churrito Bunnito", "Baskito", "Los Bunitos", "Easter Easter Easter Sahur", "Eggdin Egg Egg Dun", "Bunny Tralala", "Buntteo", "Bunny Bunny Bunny Sahur", "Noo my Eggs"}
local ESP_Enabled = false
local CurrentTab = "Features GH"

-- --- 1. LOGO & MAIN FRAME ---
local LogoButton = Instance.new("TextButton", ScreenGui)
LogoButton.Size = UDim2.new(0, 50, 0, 50)
LogoButton.Position = UDim2.new(0.5, -25, 0, 50)
LogoButton.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
LogoButton.Text = "GH"
LogoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoButton.Font = Enum.Font.GothamBold
LogoButton.TextSize = 20
LogoButton.ZIndex = 1000
Instance.new("UICorner", LogoButton).CornerRadius = UDim.new(0, 8)
local LogoStroke = Instance.new("UIStroke", LogoButton)
LogoStroke.Thickness = 2

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
MainFrame.Position = UDim2.new(0.5, -225, 0.5, -140)
MainFrame.Size = UDim2.new(0, 450, 0, 280)
MainFrame.Visible = false
MainFrame.ZIndex = 100
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Thickness = 2

local CenterTitle = Instance.new("TextLabel", MainFrame)
CenterTitle.Size = UDim2.new(1, 0, 0, 45)
CenterTitle.BackgroundTransparency = 1
CenterTitle.Text = "GURITA-HUB"
CenterTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
CenterTitle.Font = Enum.Font.GothamBold
CenterTitle.TextSize = 18
CenterTitle.ZIndex = 150

-- --- 2. SIDEBAR ---
local Sidebar = Instance.new("Frame", MainFrame)
Sidebar.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
Sidebar.Position = UDim2.new(0, 10, 0, 45)
Sidebar.Size = UDim2.new(0, 130, 0, 225)
Sidebar.ZIndex = 110
Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 8)

local SidebarLayout = Instance.new("UIListLayout", Sidebar)
SidebarLayout.Padding = UDim.new(0, 5)
SidebarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- --- 3. TAB CONTAINER (Area Kanan) ---
local Tabs = Instance.new("Frame", MainFrame)
Tabs.Name = "TabContainer"
Tabs.Position = UDim2.new(0, 150, 0, 45)
Tabs.Size = UDim2.new(1, -160, 0, 225)
Tabs.BackgroundTransparency = 1

local function createTabPage(name)
    local page = Instance.new("ScrollingFrame", Tabs)
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.Visible = false
    page.ScrollBarThickness = 2
    local l = Instance.new("UIListLayout", page)
    l.Padding = UDim.new(0, 8)
    l.HorizontalAlignment = Enum.HorizontalAlignment.Center
    return page
end

-- Membuat Halaman per Tab
local PageFeatures = createTabPage("Features GH")
local PageProtection = createTabPage("Protection GH")
local PageESP = createTabPage("ESP GH")
local PagePaskah = createTabPage("Paskah Event GH")

-- --- 4. LOGIKA PINDAH TAB ---
local function showTab(tabName)
    for _, child in pairs(Tabs:GetChildren()) do
        child.Visible = (child.Name == tabName)
    end
end

local function addSideBtn(name)
    local b = Instance.new("TextButton", Sidebar)
    b.Size = UDim2.new(0.9, 0, 0, 35)
    b.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    b.Text = name
    b.TextColor3 = Color3.fromRGB(200, 200, 200)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 10
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
    
    b.MouseButton1Click:Connect(function()
        showTab(name)
    end)
end

addSideBtn("Features GH")
addSideBtn("Protection GH")
addSideBtn("ESP GH")
addSideBtn("Paskah Event GH")

showTab("Features GH") -- Default awal

-- --- 5. ISI FITUR PASKAH ---
local function createToggle(parent, text, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(0.9, 0, 0, 40)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.Text = text .. " GH: OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    local state = false
    btn.MouseButton1Click:Connect(function()
        state = not state
        btn.Text = text .. " GH: " .. (state and "ON" or "OFF")
        btn.TextColor3 = state and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 255, 255)
        callback(state)
    end)
end

createToggle(PagePaskah, "Easter ESP", function(val)
    ESP_Enabled = val
end)

-- --- 6. ESP LOGIC ---
task.spawn(function()
    while task.wait(1) do
        if ESP_Enabled then
            for _, obj in pairs(workspace:GetDescendants()) do
                for _, egg in pairs(EasterList) do
                    if obj.Name == egg and (obj:IsA("BasePart") or obj:IsA("Model")) then
                        local target = obj:IsA("Model") and obj.PrimaryPart or obj
                        if target and not target:FindFirstChild("GH_ESP") then
                            local b = Instance.new("BillboardGui", target)
                            b.Name = "GH_ESP"
                            b.AlwaysOnTop = true
                            b.Size = UDim2.new(0, 100, 0, 50)
                            local t = Instance.new("TextLabel", b)
                            t.BackgroundTransparency = 1; t.Size = UDim2.new(1,0,1,0); t.Text = egg.." GH"; t.TextColor3 = Color3.fromRGB(255,255,0); t.Font = Enum.Font.GothamBold; t.TextSize = 12
                        end
                    end
                end
            end
        else
            for _, v in pairs(workspace:GetDescendants()) do if v.Name == "GH_ESP" then v:Destroy() end end
        end
    end
end)

-- --- 7. EXTRA (DRAG, RGB, CLOSE) ---
local function Drag(ui)
    local d, i, s, sp
    ui.InputBegan:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 then d = true; s = inp.Position; sp = ui.Position end end)
    ui.InputChanged:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseMovement then i = inp end end)
    UserInputService.InputEnded:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 then d = false end end)
    RunService.Heartbeat:Connect(function() if d and i then local delta = i.Position - s; ui.Position = UDim2.new(sp.X.Scale, sp.X.Offset + delta.X, sp.Y.Scale, sp.Y.Offset + delta.Y) end end)
end
Drag(MainFrame); Drag(LogoButton)

task.spawn(function()
    local cols = {Color3.fromRGB(0, 160, 255), Color3.fromRGB(255, 120, 0), Color3.fromRGB(255, 210, 0), Color3.fromRGB(160, 0, 255)}
    local c = 1
    while true do
        local n = c % #cols + 1
        for i = 0, 1, 0.05 do
            local l = cols[c]:Lerp(cols[n], i)
            MainStroke.Color = l; LogoStroke.Color = l
            task.wait(0.05)
        end
        c = n
    end
end)

LogoButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
local X = Instance.new("TextButton", MainFrame)
X.Size = UDim2.new(0, 25, 0, 25); X.Position = UDim2.new(1, -30, 0, 10); X.Text = "X"; X.BackgroundColor3 = Color3.fromRGB(150, 0, 0); X.TextColor3 = Color3.fromRGB(255, 255, 255); X.ZIndex = 200; Instance.new("UICorner", X).CornerRadius = UDim.new(0, 6)
X.MouseButton1Click:Connect(function() MainFrame.Visible = false end)
