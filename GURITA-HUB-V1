-- [[ GURITA HUB - MANUAL NOTIFIER TESTER ]] --

local Players = game:GetService("Players")
local lp = Players.LocalPlayer
local PlayerGui = lp:WaitForChild("PlayerGui")
local StarterGui = game:GetService("StarterGui")

-- 1. LIST TARGET
local TargetEggs = {
    "hydra bunny", "noo my eggs", "baskito", "churrito", 
    "quackini snackini", "hopilikalika hopilikalako", "bunny sahur"
}

_G.ManualScanActive = false

-- 2. FUNGSI NOTIFIKASI
local function KirimNotif(namaTelur)
    StarterGui:SetCore("SendNotification", {
        Title = "🥚 EGG DETECTED!";
        Text = "Ditemukan: " .. namaTelur:upper();
        Duration = 5;
    })
    
    local s = Instance.new("Sound", workspace)
    s.SoundId = "rbxassetid://4590662762"
    s.Volume = 1
    s:Play()
    task.wait(2)
    s:Destroy()
end

-- 3. UI MENU & LOGO
if PlayerGui:FindFirstChild("GHS_ManualNotif") then PlayerGui.GHS_ManualNotif:Destroy() end
local SG = Instance.new("ScreenGui", PlayerGui); SG.Name = "GHS_ManualNotif"; SG.ResetOnSpawn = false

-- Logo G H (Bisa Digeser)
local Logo = Instance.new("TextButton", SG)
Logo.Size = UDim2.new(0, 60, 0, 60); Logo.Position = UDim2.new(0, 10, 0.5, 0)
Logo.BackgroundColor3 = Color3.fromRGB(20, 20, 20); Logo.Text = "G H"; Logo.TextColor3 = Color3.new(1, 1, 0)
Logo.Font = Enum.Font.GothamBlack; Logo.TextSize = 20; Logo.Draggable = true; Logo.Active = true
Instance.new("UICorner", Logo); Instance.new("UIStroke", Logo).Thickness = 2

-- Frame Menu
local Main = Instance.new("Frame", SG)
Main.Size = UDim2.new(0, 220, 0, 180); Main.Position = UDim2.new(0.5, -110, 0.5, -90)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15); Main.Visible = false; Instance.new("UICorner", Main)
Instance.new("UIStroke", Main).Color = Color3.new(1, 1, 0)

Logo.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 40); Title.Text = "MANUAL SCANNER"; Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.GothamBold; Title.BackgroundTransparency = 1; Title.TextSize = 14

-- TOMBOL SCAN MANUAL (Sekali Klik, Sekali Scan)
local BtnScan = Instance.new("TextButton", Main)
BtnScan.Size = UDim2.new(0.9, 0, 0, 50); BtnScan.Position = UDim2.new(0.05, 0, 0.3, 0)
BtnScan.BackgroundColor3 = Color3.fromRGB(0, 85, 255); BtnScan.Text = "SCAN MAP SEKARANG"; BtnScan.TextColor3 = Color3.new(1,1,1)
BtnScan.Font = Enum.Font.GothamBold; BtnScan.TextSize = 12; Instance.new("UICorner", BtnScan)

-- TOMBOL TES NOTIF (Hanya untuk Tes Suara/Pop-up)
local BtnTest = Instance.new("TextButton", Main)
BtnTest.Size = UDim2.new(0.9, 0, 0, 40); BtnTest.Position = UDim2.new(0.05, 0, 0.65, 0)
BtnTest.BackgroundColor3 = Color3.fromRGB(40, 40, 40); BtnTest.Text = "TES NOTIF (SIMULASI)"; BtnTest.TextColor3 = Color3.new(1,1,1)
BtnTest.Font = Enum.Font.GothamBold
