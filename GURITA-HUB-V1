-- [[ GURITA HUB V4.2 - STABLE EDITION ]] --

local Players = game:GetService("Players")
local lp = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local CG = game:GetService("CoreGui")

-- 1. DATABASE & CONFIG
local EggNames = {"Hydra Bunny", "Noo my Eggs", "Bunny Sahur", "Easter", "Eggdin", "Buntteo", "Baskito", "Churrito", "Tralala", "Bunny Bunny", "Dun Egg", "Quackini Snackini", "Hopilikalika Hopilikalako"}
local Inceran = {["Hydra Bunny"]=true, ["Noo my Eggs"]=true, ["Baskito"]=true, ["Churrito"]=true, ["Quackini Snackini"]=true, ["Hopilikalika Hopilikalako"]=true}

_G.ESP_Player_GH = false
_G.Xray_GH = false
_G.AutoCollect_GH = false
_G.ESP_Egg_GH = false

-- 2. BACKEND LOGIC (FLAT COLOR & NOTIF)
local function IsPlayer(v)
    return v:IsDescendantOf(lp.Character) or (v.Parent and v.Parent:FindFirstChild("Humanoid"))
end

local function ApplyFlat(v)
    pcall(function()
        if (v:IsA("BasePart") or v:IsA("MeshPart")) and not IsPlayer(v) then
            v.Material = Enum.Material.Plastic
            v.CastShadow = false
            if v:IsA("MeshPart") then v.TextureID = "" end
            for _, tex in pairs(v:GetChildren()) do
                if tex:IsA("Texture") or tex:IsA("Decal") then tex:Destroy() end
            end
        end
    end)
end

local function CustomNotif(eggName)
    local sg = lp.PlayerGui:FindFirstChild("EHS_Notif") or Instance.new("ScreenGui", lp.PlayerGui)
    sg.Name = "EHS_Notif"
    local frame = Instance.new("Frame", sg)
    frame.Size = UDim2.new(0, 240, 0, 40); frame.Position = UDim2.new(0.5, -120, -0.1, 0)
    frame.BackgroundColor3 = Color3.fromRGB(15, 15, 25); frame.BorderSizePixel = 0
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
    local stroke = Instance.new("UIStroke", frame); stroke.Color = Color3.fromRGB(0, 150, 255); stroke.Thickness = 2
    local txt = Instance.new("TextLabel", frame)
    txt.Size = UDim2.new(1, 0, 1, 0); txt.BackgroundTransparency = 1; txt.Font = Enum.Font.GothamBold; txt.TextSize = 10; txt.TextColor3 = Color3.new(1, 1, 1); txt.Text = "📩 FOUND: " .. eggName:upper()
    local sound = Instance.new("Sound", workspace); sound.SoundId = "rbxassetid://4835664299"; sound.Volume = 1; sound:Play()
    game:GetService("Debris"):AddItem(sound, 2)
    frame:TweenPosition(UDim2.new(0.5, -120, 0.05, 0), "Out", "Back", 0.5, true)
    task.delay(4, function() if frame then frame:TweenPosition(UDim2.new(0.5, -120, -0.2, 0), "In", "Linear", 0.5, true); task.wait(0.5); frame:Destroy() end end)
end

-- 3. ESP PLAYER LOGIC (FIXED)
local function CreatePlayerESP(p)
    task.spawn(function()
        while p and p.Parent do
            if _G.ESP_Player_GH and p ~= lp and p.Character and p.Character:FindFirstChild("Head") then
                local head = p.Character.Head
                if not head:FindFirstChild("ESP_PlayerTag") then
                    local bg = Instance.new("BillboardGui", head)
                    bg.Name = "ESP_PlayerTag"; bg.AlwaysOnTop = true; bg.Size = UDim2.new(0, 200, 0, 50); bg.ExtentsOffset = Vector3.new(0, 3, 0)
                    local lbl = Instance.new("TextLabel", bg)
                    lbl.Size = UDim2.new(1, 0, 1, 0); lbl.BackgroundTransparency = 1; lbl.TextColor3 = Color3.new(1, 1, 1)
                    lbl.Text = p.Name; lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 16; lbl.TextStrokeTransparency = 0.5
                end
            else
                if p.Character and p.Character:FindFirstChild("Head") and p.Character.Head:FindFirstChild("ESP_PlayerTag") then
                    p.Character.Head.ESP_PlayerTag:Destroy()
                end
            end
            task.wait(1)
        end
    end)
end

-- 4. UI CONSTRUCTION
if CG:FindFirstChild("GuritaHub_BlueV42") then CG.GuritaHub_BlueV42:Destroy() end
local SG = Instance.new("ScreenGui", CG); SG.Name = "GuritaHub_BlueV42"

local LogoFrame = Instance.new("Frame", SG)
LogoFrame.Size = UDim2.new(0, 50, 0, 50); LogoFrame.Position = UDim2.new(0, 15, 0.45, 0)
LogoFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 20); LogoFrame.Active = true; LogoFrame.Draggable = true
Instance.new("UICorner", LogoFrame).CornerRadius = UDim.new(1, 0)
local LogoStroke = Instance.new("UIStroke", LogoFrame); LogoStroke.Color = Color3.fromRGB(0, 150, 255); LogoStroke.Thickness = 2
local LogoBtn = Instance.new("TextButton", LogoFrame)
LogoBtn.Size = UDim2.new(1, 0, 1, 0); LogoBtn.BackgroundTransparency = 1; LogoBtn.Text = "GH"; LogoBtn.TextColor3 = Color3.new(1, 1, 1); LogoBtn.Font = Enum.Font.GothamBold; LogoBtn.TextSize = 18

local Main = Instance.new("Frame", SG)
Main.Size = UDim2.new(0, 420, 0, 260); Main.Position = UDim2.new(0.5, -210, 0.5, -130); Main.BackgroundColor3 = Color3.fromRGB(12, 12, 18); Main.Visible = false; Main.Active = true; Main.Draggable = true
Instance.new("UICorner", Main)
local MainStroke = Instance.new("UIStroke", Main); MainStroke.Color = Color3.fromRGB(0, 150, 255); MainStroke.Thickness = 2

LogoBtn.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)

local Sidebar = Instance.new("Frame", Main)
Sidebar.Size = UDim2.new(0, 120, 1, 0); Sidebar.BackgroundColor3 = Color3.fromRGB(18, 18, 28); Sidebar.BorderSizePixel = 0; Instance.new("UICorner", Sidebar)
local SideLayout = Instance.new("UIListLayout", Sidebar); SideLayout.Padding = UDim.new(0, 5); SideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local Pages = Instance.new("Frame", Main)
Pages.Size = UDim2.new(1, -130, 1, -20); Pages.Position = UDim2.new(0, 125, 0, 10); Pages.BackgroundTransparency = 1

local function CreatePage(name)
    local p = Instance.new("ScrollingFrame", Pages); p.Name = name; p.Size = UDim2.new(1, 0, 1, 0); p.BackgroundTransparency = 1; p.Visible = false; p.ScrollBarThickness = 2
    Instance.new("UIListLayout", p).Padding = UDim.new(0, 8)
    return p
end

local PagePaska = CreatePage("PASKA"); local PageESP = CreatePage("ESP")
PagePaska.Visible = true

local function CreateSideBtn(txt, page)
    local b = Instance.new("TextButton", Sidebar)
    b.Size = UDim2.new(0.9, 0, 0, 35); b.BackgroundColor3 = Color3.fromRGB(25, 25, 40); b.Text = txt; b.TextColor3 = Color3.new(1, 1, 1); b.Font = Enum.Font.GothamBold; b.TextSize = 11; Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function() for _, v in pairs(Pages:GetChildren()) do v.Visible = false end; page.Visible = true end)
end
CreateSideBtn("PASKA EVENT", PagePaska); CreateSideBtn("ESP", PageESP)

local function CreateToggle(parent, txt, var, callback)
    local b = Instance.new("TextButton", parent)
    b.Size = UDim2.new(0.95, 0, 0, 40); b.BackgroundColor3 = Color3.fromRGB(22, 22, 35); b.Text = txt..": OFF"; b.TextColor3 = Color3.new(1, 0, 0); b.Font = Enum.Font.GothamBold; b.TextSize = 10; Instance.new("UICorner", b)
    local s = Instance.new("UIStroke", b); s.Color = Color3.fromRGB(40, 40, 60); s.Thickness = 1
    b.MouseButton1Click:Connect(function()
        _G[var] = not _G[var]
        b.Text = txt..(_G[var] and ": ON" or ": OFF")
        b.TextColor3 = _G[var] and Color3.new(0, 1, 0) or Color3.new(1, 0, 0)
        s.Color = _G[var] and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(40, 40, 60)
        callback()
    end)
end

-- [[ FITUR ]] --
CreateToggle(PagePaska, "Auto Collect Koin Paska GH", "AutoCollect_GH", function()
    task.spawn(function()
        while _G.AutoCollect_GH do
            local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                for _, v in ipairs(workspace:GetChildren()) do
                    if v:IsA("BasePart") and v.Name:lower():find("coin") and not (v.Parent and v.Parent.Name:lower():find("base")) then
                        hrp.CFrame = v.CFrame
                        firetouchinterest(hrp, v, 0); task.wait(); firetouchinterest(hrp, v, 1)
                    end
                end
            end
            task.wait(0.3)
        end
    end)
end)

CreateToggle(PagePaska, "ESP EGG HUNT GH", "ESP_Egg_GH", function()
    if not _G.ESP_Egg_GH then
        for _, v in pairs(workspace:GetDescendants()) do if v.Name == "EHSTag" then v:Destroy() end end
    end
end)

CreateToggle(PageESP, "ESP Player GH", "ESP_Player_GH", function() end)

CreateToggle(PageESP, "X-Ray GH", "Xray_GH", function()
    for _, v in ipairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") and not IsPlayer(v) then
            local n = v.Name:lower()
            if n:find("wall") or n:find("base") or n:find("door") or n:find("floor") then
                v.Transparency = _G.Xray_GH and 0.6 or 0
            end
        end
    end
end)

-- [[ AUTO RUNNERS ]] --
for _, p in pairs(Players:GetPlayers()) do CreatePlayerESP(p) end
Players.PlayerAdded:Connect(CreatePlayerESP)

task.spawn(function()
    local d = workspace:GetDescendants()
    for i = 1, #d do ApplyFlat(d[i]); if i % 400 == 0 then task.wait() end end
    workspace.DescendantAdded:Connect(ApplyFlat)
    
    while true do
        if _G.ESP_Egg_GH then
            for _, v in ipairs(workspace:GetChildren()) do
                local n = v.Name:lower()
                for _, egg in ipairs(EggNames) do
                    if n:find(egg:lower()) then
                        local t = v:IsA("Model") and (v:FindFirstChild("Handle") or v:FindFirstChildWhichIsA("BasePart")) or v
                        if t and not t:FindFirstChild("EHSTag") then
                            local bg = Instance.new("BillboardGui", t); bg.Name = "EHSTag"; bg.AlwaysOnTop = true; bg.Size = UDim2.new(0, 150, 0, 40); bg.ExtentsOffset = Vector3.new(0, 3, 0)
                            local lbl = Instance.new("TextLabel", bg); lbl.Size = UDim2.new(1, 0, 1, 0); lbl.BackgroundTransparency = 1; lbl.RichText = true
                            lbl.Text = "<b>"..egg.."</b>"; lbl.TextColor3 = Color3.fromRGB(0, 220, 255); lbl.TextSize = 13; lbl.Font = Enum.Font.GothamBold
                            if Inceran[egg] then CustomNotif(egg) end
                        end
                    end
                end
            end
        end
        task.wait(5)
    end
end)
