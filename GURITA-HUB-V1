-- [[ GURITA HUB V5.5 - NOTIF EGG SPANN GH EDITION ]] --

-- Layanan yang dibutuhkan
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local lp = Players.LocalPlayer
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local placeId = game.PlaceId
local SEARCH_DELAY = 5 

-- VARIABEL GLOBAL UNTUK FITUR ESP & NOTIF
_G.HuntingMode = true
_G.NotifMode = true

local NotifHistory = {}
local NotifQuery = {}

-- Daftar Target Item/Buah/Egg
local SuperRare = {"hydra", "no my egg", "los bunitox", "baskito", "churrito", "hopilikalika", "quackini", "harp"}
local NormalTargets = {"tralala", "eggdin", "easter", "sahur", "sky egg", "buntto egg"}

-- NAMA FILE UNTUK MENYIMPAN SETTINGAN SECARA PERMANEN DI EXECUTOR
local CONFIG_FILE = "BloxFruits_AutoHop_Config.json"

-- PENGATURAN DEFAULT
local config = {
	autoHopActive = false,
	selectedTeam = "Pirates"
}

-- LOAD CONFIGURASI DARI FILE EXECUTOR
if writefile and readfile and isfile then
	if isfile(CONFIG_FILE) then
		local success, decoded = pcall(function()
			return HttpService:JSONDecode(readfile(CONFIG_FILE))
		end)
		if success and type(decoded) == "table" then
			config = decoded
		end
	else
		writefile(CONFIG_FILE, HttpService:JSONEncode(config))
	end
end

local function saveConfig()
	if writefile then
		pcall(function()
			writefile(CONFIG_FILE, HttpService:JSONEncode(config))
		end)
	end
end

-- Cek TeleportData agar status tetap terbawa saat hop server
local teleportData = TeleportService:GetLocalPlayerTeleportData()
if teleportData and type(teleportData) == "table" and teleportData.autoHop ~= nil then
	config.autoHopActive = teleportData.autoHop
end

-- 1. PROTEKSI
local function isProtected(v)
	if v:IsDescendantOf(lp.Character) then return true end
	if v:IsA("Sky") or v:IsA("Clouds") or v:IsA("Atmosphere") then return true end
	return false
end

-- 2. LIGHTING & OPTIMIZE (SOFT)
local function SmoothOptimize()
	task.spawn(function()
		settings().Rendering.QualityLevel = 1
		lighting.GlobalShadows = false
		lighting.Brightness = 1
		lighting.Ambient = Color3.fromRGB(130, 130, 130)
		lighting.OutdoorAmbient = Color3.fromRGB(130, 130, 130)
		for _, v in pairs(workspace:GetDescendants()) do
			if i % 300 == 0 then task.wait() end
			if v:IsA("BasePart") then v.Material = Enum.Material.Plastic; v.Reflectance = 0
			elseif v:IsA("Decal") or v:IsA("Texture") then v:Destroy() end
		end
	end)
end

-- 3. NOTIF SYSTEM (POPUP + SOUND)
local function ShowPopup(objName)
	task.spawn(function()
		local sg = Instance.new("ScreenGui", player:WaitForChild("PlayerGui")); sg.Name = "GuritaNotif"; sg.ResetOnSpawn = false
		local f = Instance.new("Frame", sg)
		f.Size = UDim2.new(0, 280, 0, 50); f.Position = UDim2.new(0.5, -140, -0.1, 0)
		f.BackgroundColor3 = Color3.fromRGB(15, 15, 15); Instance.new("UICorner", f)
		local s = Instance.new("UIStroke", f); s.Color = Color3.fromRGB(0, 170, 255); s.Thickness = 2

		local t = Instance.new("TextLabel", f)
		t.Size = UDim2.new(1, 0, 1, 0); t.BackgroundTransparency = 1; t.TextColor3 = Color3.new(1,1,1)
		t.Text = "RARE SPANN: " .. objName:upper(); t.Font = "GothamBlack"; t.TextSize = 14

		local snd = Instance.new("Sound", workspace)
		snd.SoundId = "rbxassetid://4500662766"; snd.Volume = 0.7; snd.Play()

		f:TweenPosition(UDim2.new(0.5, -140, 0.05, 0), "Out", "Back", 0.5)
		task.wait(3.5)
		f:TweenPosition(UDim2.new(0.5, -140, -0.1, 0), "In", "Quad", 0.5)
		task.wait(0.6); sg:Destroy(); snd:Destroy()
	end)
end

-- Runner Antrian Notif
task.spawn(function()
	while true do
		if #NotifQueue > 0 then
			local nextObj = table.remove(NotifQueue, 1)
			ShowPopup(nextObj)
			task.wait(4.5)
		end
		task.wait(0.5)
	end
end)

-- 4. ESP LOGIC
local function PasangESP(obj)
	if not _G.HuntingMode or obj:FindFirstChild("GuritaTag") then return end
	task.spawn(function()
		task.wait(1.5)
		local name = obj.Name:lower()
		if name == "part" or name == "model" then return end
		local pos = (obj:IsA("Model") and obj.PrimaryPart and obj.PrimaryPart.Position or (obj:IsA("BasePart") and obj.Position))
		if not pos or pos.Y < 100 then return end

		local tipe = nil
		for _, r in pairs(SuperRare) do if name:find(r) then tipe = "rare"; break end end
		if not tipe then
			for _, e in pairs(NormalTargets) do if name:find(e) then tipe = "normal"; break end end
		end
		if not tipe then return end

		if tipe == "rare" and _G.NotifMode and not NotifHistory[obj.Name] then
			NotifHistory[obj.Name] = true
			table.insert(NotifQueue, obj.Name)
		end

		local col = (tipe == "rare" and Color3.new(1, 0, 0) or Color3.fromRGB(0, 170, 255))
		local b = Instance.new("BillboardGui", obj)
		b.Name = "GuritaTag"; b.Size = UDim2.new(0, 180, 0, 45); b.AlwaysOnTop = true; b.MaxDistance = math.huge

		local t = Instance.new("TextLabel", b)
		t.Size = UDim2.new(1, 0, 1, 0); t.BackgroundTransparency = 1; t.TextColor3 = col
		t.Font = "GothamBlack"; t.TextSize = 12; t.TextStrokeTransparency = 0.3

		while obj and obj.Parent and _G.HuntingMode do
			local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
			if hrp then t.Text = obj.Name:upper() .. "\n[" .. math.floor((hrp.Position - pos).Magnitude) .. "m]" end
			task.wait(2)
		end
		if b then b:Destroy() end
	end)
end

-- ==========================================================
-- MENU GUI & AUTO JOIN TIM
-- ==========================================================
local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
screenGui.Name = "BloxFruitsAutoHopGUI"
screenGui.ResetOnSpawn = false

local Toggle = Instance.new("TextButton", screenGui)
Toggle.Size = UDim2.new(0, 80, 0, 40); Toggle.Position = UDim2.new(0, 15, 0.45, 0)
Toggle.BackgroundTransparency = 1; Toggle.Text = "G H"; Toggle.TextColor3 = Color3.fromRGB(0, 170, 255)
Toggle.Font = "GothamBlack"; Toggle.TextSize = 32

local Main = Instance.new("Frame", screenGui)
Main.Size = UDim2.new(0, 250, 0, 240); Main.Position = UDim2.new(0.5, -125, 0.5, -120)
Main.BackgroundColor3 = Color3.fromRGB(10, 10, 10); Main.Visible = false; Instance.new("UICorner", Main)
local Stroke = Instance.new("UIStroke", Main); Stroke.Color = Color3.fromRGB(0, 170, 255); Stroke.Thickness = 2

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 60); Title.Position = UDim2.new(0,0,0,10)
Title.Text = "GURITA HUB\nEGG HUNT NOTIF EGG SPANN GH"
Title.Font = "GothamBlack"; Title.TextSize = 13; Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.fromRGB(0, 170, 255)

local function CreateBtn(pos)
	local b = Instance.new("TextButton", Main)
	b.Size = UDim2.new(0, 210, 0, 50); b.Position = pos; b.Font = "GothamBlack"; b.TextSize = 13
	b.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", b)
	local s = Instance.new("UIStroke", b); s.Color = Color3.fromRGB(0, 170, 255); s.Thickness = 1.2
	return b
end

local BtnESP = CreateBtn(UDim2.new(0, 20, 0, 80))
local BtnNotif = CreateBtn(UDim2.new(0, 20, 0, 150))

local function UpdateUI()
	BtnESP.Text = "ESP EGG: " .. (_G.HuntingMode and "ON" or "OFF")
	BtnESP.BackgroundColor3 = _G.HuntingMode and Color3.fromRGB(0, 80, 0) or Color3.fromRGB(80, 0, 0)
	BtnNotif.Text = "NOTIF SPAWN: " .. (_G.NotifMode and "ON" or "OFF")
	BtnNotif.BackgroundColor3 = _G.NotifMode and Color3.fromRGB(0, 80, 0) or Color3.fromRGB(80, 0, 0)
end
UpdateUI()

Toggle.MouseButton1Down:Connect(function() Main.Visible = not Main.Visible end)
BtnESP.MouseButton1Down:Connect(function()
	_G.HuntingMode = not _G.HuntingMode; UpdateUI()
	if _G.HuntingMode then for _,v in pairs(workspace:GetDescendants()) do PasangESP(v) end end
end)
BtnNotif.MouseButton1Down:Connect(function() _G.NotifMode = not _G.NotifMode; UpdateUI() end)

-- ==========================================================
-- AUTO SELECT TEAM
-- ==========================================================
local function autoSelectTeam()
	local re = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
	if re then
		pcall(function() re:InvokeServer("SetTeam", config.selectedTeam) end)
	end
end

-- ==========================================================
-- FUNGSI PENCARIAN BUAH & AUTO HOP
-- ==========================================================
local function findFruit()
	for _, obj in ipairs(Workspace:GetChildren()) do
		if obj:IsA("Tool") and (obj.Name:find("Fruit") or obj:FindFirstChild("Handle")) then
			return obj
		end
	end
	return nil
end

local function startFruitSearch()
	if not config.autoHopActive then return end

	print("[Blox Fruits] Masuk ke server, merapikan tim...")
	task.wait(2)
	autoSelectTeam()
	task.wait(2)

	local detectedFruit = findFruit()
	if detectedFruit then
		print("[SUKSES] Buah ditemukan di server ini!")
		config.autoHopActive = false
		saveConfig()
		if player.Character and player.Character:FindFirstChild("HumanoidRootPart") and detectedFruit:FindFirstChild("Handle") then
			player.Character.HumanoidRootPart.CFrame = detectedFruit.Handle.CFrame + Vector3.new(0, 5, 0)
		end
		return
	end

	print("[Blox Fruits] Buah tidak ada. Melakukan Auto Hop...")
	local success, result = pcall(function()
		local url = "https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?sortOrder=Asc&limit=100"
		return HttpService:JSONDecode(game:HttpGet(url))
	end)

	if success and result and result.data then
		local servers = {}
		for _, server in ipairs(result.data) do
			if server.playing < server.maxPlayers and server.id ~= game.JobId then
				table.insert(servers, server.id)
			end
		end

		if #servers > 0 then
			local randomServerId = servers[math.random(1, #servers)]
			TeleportService:TeleportToPlaceInstance(placeId, randomServerId, player, { autoHop = true })
		else
			task.wait(SEARCH_DELAY)
			startFruitSearch()
		end
	else
		task.wait(SEARCH_DELAY)
		startFruitSearch()
	end
end

-- Jalankan ESP dan Handler Awal
SmoothOptimize()
for _, d in ipairs(workspace:GetDescendants()) do PasangESP(d) end
workspace.DescendantAdded:Connect(function(v) PasangESP(v) end)

if config.autoHopActive then
	task.spawn(function() startFruitSearch() end)
else
	task.spawn(function() task.wait(2); autoSelectTeam() end)
end
